import { TMutations, TQueries } from '../graphqlTypes'
import { TGraphqlConfig } from 'lib/services/graphqlAPI'

__imports__

export const mutations: { [key in keyof TMutations]: TGraphqlConfig } = {__mutations__}

export const queries: { [key in keyof TQueries]: TGraphqlConfig } = {__queries__}
