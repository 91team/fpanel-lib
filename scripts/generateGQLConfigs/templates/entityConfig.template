import {
  __imports__
} from 'lib/gql/generated/types'

import { TMutations, TQueries } from '../../graphqlTypes/__entity_name__'

const mutations: {
  [key in keyof TMutations]: App.TGraphqlConfig
} = {__mutations_actions__}

const queries: {
  [key in keyof TQueries]: App.TGraphqlConfig
} = {__queries_actions__}

export { mutations, queries }
