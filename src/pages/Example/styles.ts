const styles = (theme: App.TTheme) => ({
  container: {
    margin: '0.625rem',
  },
  title: {
    marginBottom: '1.25rem',
  },
  button: {
    backgroundColor: '#0f0',
  },
})

export default styles
